manifestVersion: subsquid.io/v0.1
name: zenlink-astar
version: 4
description: |-
  Zenlink Astar Squid
build:

deploy:
  addons:
    postgres:
  processor:
    cmd: [ "node", "lib/processor" ]
  api:
    cmd: [ "npx", "squid-graphql-server"]
